name: Test with JUnit on Merge to Dev

on:
  pull_request:
    # branches:
      # - dev

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    # - name: Checkout code
    #   uses: actions/checkout@v3
    #
    # - name: Set up JDK 11
    #   uses: actions/setup-java@v3
    #   with:
    #     distribution: 'adopt'
    #     java-version: '11'
    #
    # - name: Build with Gradle
      # run: ./gradlew build

    # - name: Run tests
      # run: ./gradlew test

    # - name: Archive test results
    #   if: always()
    #   uses: actions/upload-artifact@v3
    #   with:
    #     name: test-results
    #     path: build/test-results
          #
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
